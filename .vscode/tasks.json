{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "ionic serve",
            "type": "shell",
            "command": "ionic serve",
            "options": {
                "cwd": "${workspaceFolder}/activity-2"
            }
        }
    ]
}